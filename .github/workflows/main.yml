name: Main cache warmup

on:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: main-cache-warmup
  cancel-in-progress: false

jobs:
  gate:
    uses: ./.github/workflows/gate.yml
